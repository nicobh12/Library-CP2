#include "structs.h"

void loginUser(const char* filename, user u);

void signInUser(const char* filename, user u);

void loginAdmin(const char* filename, admin a);

void signInAdmin(const char* filename, admin a);

void encryptPass(const char* filename, char* pass);